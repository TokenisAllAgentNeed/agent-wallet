/**
 * Agent-Wallet constants — extracted from inline magic numbers.
 */

/** EVM fallback gas estimate: 100k gas units * 30 gwei */
export const EVM_FALLBACK_GAS_ESTIMATE = 100_000n * 30_000_000_000n;

/** Tron conservative gas estimate (15 TRX in sun) */
export const TRON_GAS_ESTIMATE = 15_000_000n;
